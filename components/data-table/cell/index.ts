/**
 * Cell Module
 * 
 * This module provides components for rendering table cells in the data table.
 * It exports the main DataTableCell component which handles:
 * - Regular cell rendering
 * - Aggregated cell rendering
 * - Custom cell renderers through a registry system
 * - Grouped cell states
 * 
 * The cell system is designed to work with TanStack Table and supports
 * extensible rendering through a plugin-based architecture.
 * 
 * @module cell
 */

export { DataTableCell } from "./cell" 